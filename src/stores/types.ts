/**
 * Re-export the Store and StoreRecord interfaces from the central types module.
 * This file exists as a convenient import path for store implementations.
 */
export type { Store, StoreRecord } from '../types';
